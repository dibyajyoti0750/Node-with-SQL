<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      :root {
        --geist-background: #000;
        --geist-foreground: #fff;
        --geist-border: #333;
        --geist-error: #ff3333;
      }
    </style>
  </head>
  <body
    class="bg-[var(--geist-background)] text-[var(--geist-foreground)] flex items-center justify-center min-h-screen"
  >
    <div
      class="w-full max-w-md p-6 border border-[var(--geist-border)] rounded-2xl bg-black shadow-lg"
    >
      <h2 class="text-2xl font-semibold mb-4 text-center">Delete Account</h2>
      <p class="text-sm text-gray-400 text-center mb-4">
        This action is irreversible.
      </p>

      <form method="POST" action="/users/<%= user.id %>?_method=DELETE">
        <label class="block mb-2 text-sm">Email</label>
        <input
          type="email"
          name="email"
          class="w-full p-2 bg-neutral-900 border border-[var(--geist-border)] rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-[var(--geist-error)]"
          placeholder="Enter your email"
          required
        />

        <label class="block mt-4 mb-2 text-sm">Password</label>
        <input
          type="password"
          name="password"
          class="w-full p-2 bg-neutral-900 border border-[var(--geist-border)] rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-[var(--geist-error)]"
          placeholder="Enter your password"
          required
        />

        <button
          type="submit"
          class="mt-6 w-full p-3 bg-[var(--geist-error)] text-white rounded-lg font-semibold hover:opacity-80 transition"
        >
          Delete Account
        </button>
      </form>
    </div>
  </body>
</html>
