<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Members</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      /* Dark theme with Geist-inspired colors */
      :root {
        --background: #171717; /* Dark gray background */
        --foreground: #fafafa; /* Light text */
        --gray-300: #d4d4d4; /* Light gray for accents */
        --gray-500: #737373; /* Mid gray for secondary elements */
        --gray-700: #404040; /* Darker gray for borders */
        --accent-gradient: linear-gradient(
          45deg,
          #3b82f6,
          #a855f7
        ); /* Blue to purple */
      }

      body {
        background-color: var(--background);
        color: var(--foreground);
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          sans-serif;
        margin: 0;
        padding: 3rem;
        min-height: 100vh;
        background: linear-gradient(
          to bottom right,
          #171717,
          #1f1f1f
        ); /* Subtle BG gradient */
      }

      h1 {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 2.5rem;
        color: var(--foreground);
        background: var(--accent-gradient);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
        text-align: center;
        letter-spacing: 0.5px;
      }

      table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0 0.75rem;
        background-color: transparent;
        margin: 0 auto;
      }

      th {
        text-align: left;
        padding: 0.8rem;
        font-weight: 500;
        color: var(--gray-300);
        border-bottom: 2px solid var(--accent-gradient);
      }

      td {
        padding: 0.8rem;
        color: var(--foreground);
        background-color: rgba(
          31,
          31,
          31,
          0.8
        ); /* Slightly lighter card effect */
      }

      tr {
        border-radius: 12px;
        overflow: hidden;
      }

      .avatar {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        object-fit: cover;
        border: 2px solid var(--accent-gradient);
        transition: transform 0.3s ease;
      }

      .avatar:hover {
        transform: scale(1.1);
      }

      /* Ensure first and last cells in row have rounded corners */
      td:first-child {
        border-top-left-radius: 12px;
        border-bottom-left-radius: 12px;
      }

      td:last-child {
        border-top-right-radius: 12px;
        border-bottom-right-radius: 12px;
      }

      .container {
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .edit {
        background: #000;
        color: #fff;
        border: none;
        padding: 5px 10px;
        font-size: 16px;
        font-weight: 500;
        border-radius: 6px;
        cursor: pointer;
        transition: all 0.2s ease-in-out;
      }

      .edit:hover {
        background: #111;
      }

      .edit:active {
        background: #222;
      }

      .delete-btn {
        background: #b91c1c;
        color: white;
        border: none;
        padding: 5px 10px;
        font-size: 16px;
        font-weight: 500;
        border-radius: 6px;
        cursor: pointer;
        transition: all 0.2s ease-in-out;
      }

      .delete-btn:hover {
        background: #991b1b;
      }

      .delete-btn:active {
        background: #7f1d1d;
      }
    </style>
  </head>
  <body>
    <div class="container d-grid gap-2">
      <h1><%= count %> Members and Counting...</h1>
      <form action="/users/new" method="get">
        <button class="btn btn-primary btn-lg fw-semibold w-100">
          Join Us Today
        </button>
      </form>

      <table>
        <tr>
          <th></th>
          <th>Username</th>
          <th>Email</th>
        </tr>
        <% for(let user of users) { %>
        <tr>
          <td><img src="<%= user.avatar %>" alt="img" class="avatar" /></td>
          <td>@<%= user.username %></td>
          <td><%= user.email %></td>
          <td>
            <form action="/users/<%= user.id %>/edit" method="get">
              <button class="edit">Edit</button>
            </form>
          </td>
          <td>
            <form action="/users/<%= user.id %>/delete" method="get">
              <button class="delete-btn">Delete</button>
            </form>
          </td>
        </tr>
        <% } %>
      </table>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
